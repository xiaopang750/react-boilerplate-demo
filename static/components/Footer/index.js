import React from 'react';
import style from './index.styl';

const Footer = ({building, idle}) => {
  return (
    <div className={style.footer}>Copyright: Thoughtworks Inc</div>
  );
};

export default Footer;
